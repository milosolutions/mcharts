#pragma once

/*!
  * defgroup GUI Gui code
  *
  * Static strings used in application. When defined in a file like this one,
  * we can be sure there will be no typos, because compiler will throw an error
  * if you mistype `Tags::coreGroup`, for example. It is also easier to
  * refactor, rename tags than searching for all occurences of a string in
  * multiple files.
  */
namespace Tags {
//const char *foo = "foo";
//const QByteArray bar = "bar";
}
